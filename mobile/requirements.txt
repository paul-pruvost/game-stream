numpy>=1.24
opencv-python>=4.8
mss>=9.0
dxcam>=0.0.5             # Windows only — DRM-compatible capture (YouTube, etc.)
pynput>=1.7
aiohttp>=3.9             # WebSocket + HTTP server
cryptography>=42.0       # TLS (optional)
zeroconf>=0.128          # mDNS service announcement & discovery
pyperclip>=1.8           # Clipboard sync (optional)
